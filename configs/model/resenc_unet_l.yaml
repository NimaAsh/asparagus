defaults:
  - core/resenc_unet@

dimensions: 3D

pretrain_net: resenc_unet_l

seg_net: resenc_unet_l
cls_net:

plugin_seg_net:
plugin_seg_train_n_last_params: 10

# Dropout rates (0.0 = no dropout)
encoder_dropout_rate: 0.0
decoder_dropout_rate: 0.0
